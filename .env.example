# Environment Configuration Example for Braude MCP Server

# Deployment environment (production / development / test)
ENVIRONMENT=production

# Maximum allowed requests per IP per 1-minute window
RATE_LIMIT_MAX=60
